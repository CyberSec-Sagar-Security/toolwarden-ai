# models

Model loading/config code only. Actual weight files (DeBERTa-v3-base checkpoints, Qwen2.5-14B-Instruct GGUF, LightGBM artifacts) are **not** stored here — they live at `D:\CyberSecurity\Projects\Apps and Models\ToolWarden` and are referenced via config/env var, never a hardcoded relative path into this repo.
